div class="panel panel-default"
  div class="panel-heading"
    h4 List products
    div class="panel-body"
      p

    table.table
      thead
        tr
          th Id
          th Name
          th Description
          th Price
          th Image
          th Category
          th Group
          th Actions
      tbody
        - @products.each do |pro|
          tr
            th scope="row" = pro&.id
            td = pro&.name
            td = pro&.description
            td = pro&.price
            td = default_image?(pro&.pictures&.first&.url_picture)
            td = pro&.category&.name
            td = pro&.group_product
            td
              = link_to "Edit product", edit_product_path(pro), class: "btn btn-warning"
              | &emsp;
              = link_to "Show product", product_path(pro), class: "btn btn-primary"
              | &emsp;
              = link_to "Delete product", product_path(pro), class: "btn btn-success", method: :delete, data: { confirm: 'Are you certain you want to delete this?' }
              | &emsp;
              = link_to "Show category", category_path(pro&.category_id), class: "btn btn-primary"
    = paginate @products
= link_to "Back to list categories", categories_path, class: "btn btn-success"
